apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-sso.fullname" . }}
  labels: {{- include "customer-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "customer-sso.selectorLabels" . | nindent 4 }}
