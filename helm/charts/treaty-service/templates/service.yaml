apiVersion: v1
kind: Service
metadata:
  name: {{ include "treaty-service.fullname" . }}
  labels: {{- include "treaty-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "treaty-service.selectorLabels" . | nindent 4 }}
