apiVersion: v1
kind: Service
metadata:
  name: {{ include "cession-service.fullname" . }}
  labels: {{- include "cession-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "cession-service.selectorLabels" . | nindent 4 }}
