apiVersion: v1
kind: Service
metadata:
  name: {{ include "premium-service.fullname" . }}
  labels: {{- include "premium-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "premium-service.selectorLabels" . | nindent 4 }}
