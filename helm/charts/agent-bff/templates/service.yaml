apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-bff.fullname" . }}
  labels: {{- include "agent-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "agent-bff.selectorLabels" . | nindent 4 }}
