apiVersion: v1
kind: Service
metadata:
  name: {{ include "uw-service.fullname" . }}
  labels: {{- include "uw-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "uw-service.selectorLabels" . | nindent 4 }}
