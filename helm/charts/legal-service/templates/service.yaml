apiVersion: v1
kind: Service
metadata:
  name: {{ include "legal-service.fullname" . }}
  labels: {{- include "legal-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "legal-service.selectorLabels" . | nindent 4 }}
