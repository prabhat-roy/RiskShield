apiVersion: v1
kind: Service
metadata:
  name: {{ include "usage-based.fullname" . }}
  labels: {{- include "usage-based.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "usage-based.selectorLabels" . | nindent 4 }}
