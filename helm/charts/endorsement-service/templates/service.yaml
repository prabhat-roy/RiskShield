apiVersion: v1
kind: Service
metadata:
  name: {{ include "endorsement-service.fullname" . }}
  labels: {{- include "endorsement-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "endorsement-service.selectorLabels" . | nindent 4 }}
