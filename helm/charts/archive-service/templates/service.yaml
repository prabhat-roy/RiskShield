apiVersion: v1
kind: Service
metadata:
  name: {{ include "archive-service.fullname" . }}
  labels: {{- include "archive-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "archive-service.selectorLabels" . | nindent 4 }}
