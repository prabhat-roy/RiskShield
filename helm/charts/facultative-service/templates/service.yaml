apiVersion: v1
kind: Service
metadata:
  name: {{ include "facultative-service.fullname" . }}
  labels: {{- include "facultative-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "facultative-service.selectorLabels" . | nindent 4 }}
