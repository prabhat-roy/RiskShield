apiVersion: v1
kind: Service
metadata:
  name: {{ include "rbac-service.fullname" . }}
  labels: {{- include "rbac-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rbac-service.selectorLabels" . | nindent 4 }}
