apiVersion: v1
kind: Service
metadata:
  name: {{ include "ifrs17-service.fullname" . }}
  labels: {{- include "ifrs17-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ifrs17-service.selectorLabels" . | nindent 4 }}
