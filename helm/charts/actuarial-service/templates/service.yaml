apiVersion: v1
kind: Service
metadata:
  name: {{ include "actuarial-service.fullname" . }}
  labels: {{- include "actuarial-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "actuarial-service.selectorLabels" . | nindent 4 }}
