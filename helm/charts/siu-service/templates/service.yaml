apiVersion: v1
kind: Service
metadata:
  name: {{ include "siu-service.fullname" . }}
  labels: {{- include "siu-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "siu-service.selectorLabels" . | nindent 4 }}
