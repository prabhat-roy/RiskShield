apiVersion: v1
kind: Service
metadata:
  name: {{ include "exclusion-service.fullname" . }}
  labels: {{- include "exclusion-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "exclusion-service.selectorLabels" . | nindent 4 }}
