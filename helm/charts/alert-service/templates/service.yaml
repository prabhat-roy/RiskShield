apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert-service.fullname" . }}
  labels: {{- include "alert-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "alert-service.selectorLabels" . | nindent 4 }}
