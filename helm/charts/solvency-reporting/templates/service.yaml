apiVersion: v1
kind: Service
metadata:
  name: {{ include "solvency-reporting.fullname" . }}
  labels: {{- include "solvency-reporting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "solvency-reporting.selectorLabels" . | nindent 4 }}
