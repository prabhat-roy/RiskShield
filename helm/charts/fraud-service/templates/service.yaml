apiVersion: v1
kind: Service
metadata:
  name: {{ include "fraud-service.fullname" . }}
  labels: {{- include "fraud-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fraud-service.selectorLabels" . | nindent 4 }}
