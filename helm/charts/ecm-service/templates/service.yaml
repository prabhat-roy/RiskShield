apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecm-service.fullname" . }}
  labels: {{- include "ecm-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ecm-service.selectorLabels" . | nindent 4 }}
