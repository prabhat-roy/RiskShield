apiVersion: v1
kind: Service
metadata:
  name: {{ include "esign-service.fullname" . }}
  labels: {{- include "esign-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "esign-service.selectorLabels" . | nindent 4 }}
