apiVersion: v1
kind: Service
metadata:
  name: {{ include "self-service.fullname" . }}
  labels: {{- include "self-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "self-service.selectorLabels" . | nindent 4 }}
