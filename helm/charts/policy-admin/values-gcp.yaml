cloud: {provider: gcp, storageClass: pd-balanced, ingressClass: gce}
serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: riskshield-policy-admin@{{ .Values.gcp.project }}.iam.gserviceaccount.com
podAnnotations: {cluster-autoscaler.kubernetes.io/safe-to-evict: "true"}
envFrom:
  - secretRef: {name: policy-admin-gcp-secrets}
