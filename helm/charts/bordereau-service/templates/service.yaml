apiVersion: v1
kind: Service
metadata:
  name: {{ include "bordereau-service.fullname" . }}
  labels: {{- include "bordereau-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "bordereau-service.selectorLabels" . | nindent 4 }}
