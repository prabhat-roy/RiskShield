apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-sso.fullname" . }}
  labels: {{- include "agent-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "agent-sso.selectorLabels" . | nindent 4 }}
