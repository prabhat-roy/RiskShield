apiVersion: v1
kind: Service
metadata:
  name: {{ include "channel-service.fullname" . }}
  labels: {{- include "channel-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "channel-service.selectorLabels" . | nindent 4 }}
