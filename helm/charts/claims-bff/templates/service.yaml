apiVersion: v1
kind: Service
metadata:
  name: {{ include "claims-bff.fullname" . }}
  labels: {{- include "claims-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - {port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http}
  selector: {{- include "claims-bff.selectorLabels" . | nindent 4 }}
