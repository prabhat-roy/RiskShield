apiVersion: v1
kind: Service
metadata:
  name: {{ include "kyc-service.fullname" . }}
  labels: {{- include "kyc-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "kyc-service.selectorLabels" . | nindent 4 }}
