cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: riskshield-repair-network@{{ .Values.gcp.project }}.iam.gserviceaccount.com
