apiVersion: v1
kind: Service
metadata:
  name: {{ include "repair-network.fullname" . }}
  labels: {{- include "repair-network.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "repair-network.selectorLabels" . | nindent 4 }}
