apiVersion: v1
kind: Service
metadata:
  name: {{ include "appetite-service.fullname" . }}
  labels: {{- include "appetite-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "appetite-service.selectorLabels" . | nindent 4 }}
