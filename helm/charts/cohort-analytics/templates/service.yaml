apiVersion: v1
kind: Service
metadata:
  name: {{ include "cohort-analytics.fullname" . }}
  labels: {{- include "cohort-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "cohort-analytics.selectorLabels" . | nindent 4 }}
