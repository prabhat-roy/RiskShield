apiVersion: v1
kind: Service
metadata:
  name: {{ include "loss-analytics.fullname" . }}
  labels: {{- include "loss-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "loss-analytics.selectorLabels" . | nindent 4 }}
