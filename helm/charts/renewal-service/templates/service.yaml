apiVersion: v1
kind: Service
metadata:
  name: {{ include "renewal-service.fullname" . }}
  labels: {{- include "renewal-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "renewal-service.selectorLabels" . | nindent 4 }}
