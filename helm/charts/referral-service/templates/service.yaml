apiVersion: v1
kind: Service
metadata:
  name: {{ include "referral-service.fullname" . }}
  labels: {{- include "referral-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "referral-service.selectorLabels" . | nindent 4 }}
