apiVersion: v1
kind: Service
metadata:
  name: {{ include "adjudication-service.fullname" . }}
  labels: {{- include "adjudication-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "adjudication-service.selectorLabels" . | nindent 4 }}
