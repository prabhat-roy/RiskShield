cloud: {provider: aws, storageClass: gp3, ingressClass: alb}
serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/riskshield-customer-bff
podAnnotations:
  iam.amazonaws.com/role: riskshield-customer-bff
envFrom:
  - secretRef: {name: customer-bff-aws-secrets}
