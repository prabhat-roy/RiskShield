name: riskshield
description: RiskShield — Pulumi alternative IaC (mirrors infra/terraform/{aws,gcp,azure})
runtime: { name: nodejs, options: { typescript: true } }
backend:
  url: s3://riskshield-pulumi-state
config:
  riskshield:env: prod
  riskshield:cloud: aws
