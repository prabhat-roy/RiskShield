catalog:
  name: riskshield_lake
  type: rest
  uri: https://nessie.riskshield.internal/iceberg
warehouse: s3://riskshield-data-lake/warehouse
io:
  s3:
    endpoint: https://s3.amazonaws.com
    region: us-east-1
table-default:
  write.format.default: parquet
  write.metadata.compression-codec: gzip
