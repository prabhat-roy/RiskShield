apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata: { name: riskshield_java_service, title: New RiskShield Java/Spring service, tags: [java, riskshield] }
spec:
  owner: platform-team
  type: service
  parameters:
    - title: Service info
      required: [name]
      properties: { name: { title: Name, type: string } }
  steps:
    - { id: fetch, name: Fetch, action: fetch:template, input: { url: ./skeletons/java } }
