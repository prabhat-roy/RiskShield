apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: riskshield-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [riskshield] }
  timeOffset: "+500ms"
  duration: 3m
