apiVersion: chaos-mesh.org/v1alpha1
kind: IOChaos
metadata: { name: riskshield-io-latency, namespace: chaos-mesh }
spec:
  action: latency
  mode: one
  selector: { namespaces: [riskshield] }
  volumePath: /var/lib/data
  delay: "100ms"
  percent: 50
  duration: 5m
