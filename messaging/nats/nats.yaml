apiVersion: nats.io/v1alpha2
kind: NatsCluster
metadata: { name: riskshield-nats, namespace: messaging }
spec:
  size: 3
  natsConfig:
    jetstream:
      enabled: true
      memStorage: { enabled: true, size: 4Gi }
      fileStorage: { enabled: true, size: 200Gi, storageClassName: gp3 }
