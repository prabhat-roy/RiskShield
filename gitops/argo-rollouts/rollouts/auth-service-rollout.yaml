apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata: { name: auth-service, namespace: riskshield }
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: { duration: 5m }
        - analysis:
            templates: [{ templateName: riskshield-canary-analysis }]
            args: [{ name: service-name, value: auth-service }]
        - setWeight: 50
        - pause: { duration: 5m }
        - setWeight: 100
  selector:
    matchLabels: { app.kubernetes.io/name: auth-service }
  template:
    metadata:
      labels: { app.kubernetes.io/name: auth-service }
    spec:
      containers:
        - name: auth-service
          image: harbor.riskshield.internal/riskshield/auth-service
          ports: [{ containerPort: 50000 }]
