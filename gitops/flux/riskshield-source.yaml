apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: riskshield, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/RiskShield.git
  ref: { branch: main }
