apiVersion: v1
kind: Service
metadata:
  name: policy-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: policy-service, riskshield.io/domain: policy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50040, targetPort: 50040 }]
  selector: { app.kubernetes.io/name: policy-service }
