apiVersion: apps/v1
kind: Deployment
metadata:
  name: endorsement-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: endorsement-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: policy
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: endorsement-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: endorsement-service, riskshield.io/domain: policy }
    spec:
      serviceAccountName: endorsement-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: endorsement-service
          image: harbor.riskshield.internal/riskshield/endorsement-service:latest
          ports: [{ containerPort: 50041, name: http }]
          envFrom: [{ configMapRef: { name: endorsement-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50041 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50041 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
