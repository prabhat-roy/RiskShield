apiVersion: v1
kind: Service
metadata:
  name: cession-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: cession-service, riskshield.io/domain: reinsurance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: cession-service }
