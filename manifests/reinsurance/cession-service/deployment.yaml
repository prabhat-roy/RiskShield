apiVersion: apps/v1
kind: Deployment
metadata:
  name: cession-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: cession-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: reinsurance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: cession-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: cession-service, riskshield.io/domain: reinsurance }
    spec:
      serviceAccountName: cession-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: cession-service
          image: harbor.riskshield.internal/riskshield/cession-service:latest
          ports: [{ containerPort: 50102, name: http }]
          envFrom: [{ configMapRef: { name: cession-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50102 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50102 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
