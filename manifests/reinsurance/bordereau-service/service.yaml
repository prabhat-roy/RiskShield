apiVersion: v1
kind: Service
metadata:
  name: bordereau-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: bordereau-service, riskshield.io/domain: reinsurance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50103, targetPort: 50103 }]
  selector: { app.kubernetes.io/name: bordereau-service }
