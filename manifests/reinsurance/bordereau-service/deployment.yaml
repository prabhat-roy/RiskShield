apiVersion: apps/v1
kind: Deployment
metadata:
  name: bordereau-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: bordereau-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: reinsurance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: bordereau-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: bordereau-service, riskshield.io/domain: reinsurance }
    spec:
      serviceAccountName: bordereau-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: bordereau-service
          image: harbor.riskshield.internal/riskshield/bordereau-service:latest
          ports: [{ containerPort: 50103, name: http }]
          envFrom: [{ configMapRef: { name: bordereau-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50103 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50103 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
