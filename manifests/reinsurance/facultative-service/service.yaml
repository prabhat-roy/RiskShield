apiVersion: v1
kind: Service
metadata:
  name: facultative-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: facultative-service, riskshield.io/domain: reinsurance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50101, targetPort: 50101 }]
  selector: { app.kubernetes.io/name: facultative-service }
