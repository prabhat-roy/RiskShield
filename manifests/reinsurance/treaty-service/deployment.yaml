apiVersion: apps/v1
kind: Deployment
metadata:
  name: treaty-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: treaty-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: reinsurance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: treaty-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: treaty-service, riskshield.io/domain: reinsurance }
    spec:
      serviceAccountName: treaty-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: treaty-service
          image: harbor.riskshield.internal/riskshield/treaty-service:latest
          ports: [{ containerPort: 50100, name: http }]
          envFrom: [{ configMapRef: { name: treaty-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50100 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50100 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
