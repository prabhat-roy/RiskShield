apiVersion: v1
kind: Service
metadata:
  name: litigation-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: litigation-service, riskshield.io/domain: claims }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50084, targetPort: 50084 }]
  selector: { app.kubernetes.io/name: litigation-service }
