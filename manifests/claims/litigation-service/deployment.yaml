apiVersion: apps/v1
kind: Deployment
metadata:
  name: litigation-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: litigation-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: claims
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: litigation-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: litigation-service, riskshield.io/domain: claims }
    spec:
      serviceAccountName: litigation-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: litigation-service
          image: harbor.riskshield.internal/riskshield/litigation-service:latest
          ports: [{ containerPort: 50084, name: http }]
          envFrom: [{ configMapRef: { name: litigation-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50084 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50084 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
