apiVersion: v1
kind: Service
metadata:
  name: claim-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: claim-service, riskshield.io/domain: claims }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50080, targetPort: 50080 }]
  selector: { app.kubernetes.io/name: claim-service }
