apiVersion: v1
kind: Service
metadata:
  name: fnol-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: fnol-service, riskshield.io/domain: claims }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50081, targetPort: 50081 }]
  selector: { app.kubernetes.io/name: fnol-service }
