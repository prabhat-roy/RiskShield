apiVersion: v1
kind: Service
metadata:
  name: premium-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: premium-service, riskshield.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: premium-service }
