apiVersion: apps/v1
kind: Deployment
metadata:
  name: premium-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: premium-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: finance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: premium-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: premium-service, riskshield.io/domain: finance }
    spec:
      serviceAccountName: premium-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: premium-service
          image: harbor.riskshield.internal/riskshield/premium-service:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: premium-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
