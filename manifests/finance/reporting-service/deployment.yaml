apiVersion: apps/v1
kind: Deployment
metadata:
  name: reporting-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: reporting-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: finance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: reporting-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: reporting-service, riskshield.io/domain: finance }
    spec:
      serviceAccountName: reporting-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: reporting-service
          image: harbor.riskshield.internal/riskshield/reporting-service:latest
          ports: [{ containerPort: 50223, name: http }]
          envFrom: [{ configMapRef: { name: reporting-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50223 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50223 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
