apiVersion: v1
kind: Service
metadata:
  name: scoring-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: scoring-service, riskshield.io/domain: telematics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50283, targetPort: 50283 }]
  selector: { app.kubernetes.io/name: scoring-service }
