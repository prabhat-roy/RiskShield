apiVersion: apps/v1
kind: Deployment
metadata:
  name: scoring-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: scoring-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: telematics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: scoring-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: scoring-service, riskshield.io/domain: telematics }
    spec:
      serviceAccountName: scoring-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: scoring-service
          image: harbor.riskshield.internal/riskshield/scoring-service:latest
          ports: [{ containerPort: 50283, name: http }]
          envFrom: [{ configMapRef: { name: scoring-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50283 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50283 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
