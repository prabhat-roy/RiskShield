apiVersion: v1
kind: Service
metadata:
  name: usage-based
  namespace: riskshield
  labels: { app.kubernetes.io/name: usage-based, riskshield.io/domain: telematics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50282, targetPort: 50282 }]
  selector: { app.kubernetes.io/name: usage-based }
