apiVersion: v1
kind: Service
metadata:
  name: telematics-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: telematics-service, riskshield.io/domain: telematics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50280, targetPort: 50280 }]
  selector: { app.kubernetes.io/name: telematics-service }
