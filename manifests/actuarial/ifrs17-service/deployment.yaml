apiVersion: apps/v1
kind: Deployment
metadata:
  name: ifrs17-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: ifrs17-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: actuarial
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: ifrs17-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: ifrs17-service, riskshield.io/domain: actuarial }
    spec:
      serviceAccountName: ifrs17-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: ifrs17-service
          image: harbor.riskshield.internal/riskshield/ifrs17-service:latest
          ports: [{ containerPort: 50123, name: http }]
          envFrom: [{ configMapRef: { name: ifrs17-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50123 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50123 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
