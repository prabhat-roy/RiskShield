apiVersion: v1
kind: Service
metadata:
  name: reserve-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: reserve-service, riskshield.io/domain: actuarial }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: reserve-service }
