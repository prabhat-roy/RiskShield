apiVersion: apps/v1
kind: Deployment
metadata:
  name: reserve-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: reserve-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: actuarial
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: reserve-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: reserve-service, riskshield.io/domain: actuarial }
    spec:
      serviceAccountName: reserve-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: reserve-service
          image: harbor.riskshield.internal/riskshield/reserve-service:latest
          ports: [{ containerPort: 50122, name: http }]
          envFrom: [{ configMapRef: { name: reserve-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50122 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50122 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
