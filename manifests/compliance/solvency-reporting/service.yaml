apiVersion: v1
kind: Service
metadata:
  name: solvency-reporting
  namespace: riskshield
  labels: { app.kubernetes.io/name: solvency-reporting, riskshield.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50240, targetPort: 50240 }]
  selector: { app.kubernetes.io/name: solvency-reporting }
