apiVersion: v1
kind: Service
metadata:
  name: kyc-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: kyc-service, riskshield.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: kyc-service }
