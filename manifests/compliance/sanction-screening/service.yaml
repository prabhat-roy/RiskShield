apiVersion: v1
kind: Service
metadata: { name: sanction-screening, namespace: riskshield, labels: { app.kubernetes.io/name: sanction-screening, riskshield.io/domain: compliance } }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 8080, targetPort: 8080 }]
  selector: { app.kubernetes.io/name: sanction-screening }
