apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: alert-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: fraud
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: alert-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: alert-service, riskshield.io/domain: fraud }
    spec:
      serviceAccountName: alert-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: alert-service
          image: harbor.riskshield.internal/riskshield/alert-service:latest
          ports: [{ containerPort: 50144, name: http }]
          envFrom: [{ configMapRef: { name: alert-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50144 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50144 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
