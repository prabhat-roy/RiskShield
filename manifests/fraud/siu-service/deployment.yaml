apiVersion: apps/v1
kind: Deployment
metadata:
  name: siu-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: siu-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: fraud
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: siu-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: siu-service, riskshield.io/domain: fraud }
    spec:
      serviceAccountName: siu-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: siu-service
          image: harbor.riskshield.internal/riskshield/siu-service:latest
          ports: [{ containerPort: 50141, name: http }]
          envFrom: [{ configMapRef: { name: siu-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
