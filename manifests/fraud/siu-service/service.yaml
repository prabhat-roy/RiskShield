apiVersion: v1
kind: Service
metadata:
  name: siu-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: siu-service, riskshield.io/domain: fraud }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: siu-service }
