apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: fraud-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: fraud
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: fraud-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: fraud-service, riskshield.io/domain: fraud }
    spec:
      serviceAccountName: fraud-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: fraud-service
          image: harbor.riskshield.internal/riskshield/fraud-service:latest
          ports: [{ containerPort: 50140, name: http }]
          envFrom: [{ configMapRef: { name: fraud-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
