apiVersion: apps/v1
kind: Deployment
metadata:
  name: appetite-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: appetite-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: underwriting
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: appetite-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: appetite-service, riskshield.io/domain: underwriting }
    spec:
      serviceAccountName: appetite-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: appetite-service
          image: harbor.riskshield.internal/riskshield/appetite-service:latest
          ports: [{ containerPort: 50063, name: http }]
          envFrom: [{ configMapRef: { name: appetite-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50063 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50063 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
