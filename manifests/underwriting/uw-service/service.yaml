apiVersion: v1
kind: Service
metadata:
  name: uw-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: uw-service, riskshield.io/domain: underwriting }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50060, targetPort: 50060 }]
  selector: { app.kubernetes.io/name: uw-service }
