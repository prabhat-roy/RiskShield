apiVersion: v1
kind: ConfigMap
metadata:
  name: geo-address-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: geo-address-service, riskshield.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51013"
