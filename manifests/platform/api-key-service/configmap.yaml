apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: api-key-service, riskshield.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
