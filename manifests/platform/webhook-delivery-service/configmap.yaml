apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-delivery-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: webhook-delivery-service, riskshield.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51004"
