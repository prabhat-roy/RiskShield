apiVersion: v1
kind: Service
metadata:
  name: tpa-gateway
  namespace: riskshield
  labels: { app.kubernetes.io/name: tpa-gateway, riskshield.io/domain: third-party }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50263, targetPort: 50263 }]
  selector: { app.kubernetes.io/name: tpa-gateway }
