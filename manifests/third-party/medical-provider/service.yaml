apiVersion: v1
kind: Service
metadata:
  name: medical-provider
  namespace: riskshield
  labels: { app.kubernetes.io/name: medical-provider, riskshield.io/domain: third-party }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: medical-provider }
