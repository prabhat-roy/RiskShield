apiVersion: v1
kind: Service
metadata:
  name: legal-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: legal-service, riskshield.io/domain: third-party }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: legal-service }
