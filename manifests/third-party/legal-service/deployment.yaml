apiVersion: apps/v1
kind: Deployment
metadata:
  name: legal-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: legal-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: third-party
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: legal-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: legal-service, riskshield.io/domain: third-party }
    spec:
      serviceAccountName: legal-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: legal-service
          image: harbor.riskshield.internal/riskshield/legal-service:latest
          ports: [{ containerPort: 50262, name: http }]
          envFrom: [{ configMapRef: { name: legal-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
