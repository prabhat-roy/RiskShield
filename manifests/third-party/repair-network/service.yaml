apiVersion: v1
kind: Service
metadata:
  name: repair-network
  namespace: riskshield
  labels: { app.kubernetes.io/name: repair-network, riskshield.io/domain: third-party }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50260, targetPort: 50260 }]
  selector: { app.kubernetes.io/name: repair-network }
