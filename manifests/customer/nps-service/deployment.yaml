apiVersion: apps/v1
kind: Deployment
metadata:
  name: nps-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: nps-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: customer
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: nps-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: nps-service, riskshield.io/domain: customer }
    spec:
      serviceAccountName: nps-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: nps-service
          image: harbor.riskshield.internal/riskshield/nps-service:latest
          ports: [{ containerPort: 50184, name: http }]
          envFrom: [{ configMapRef: { name: nps-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50184 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50184 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
