apiVersion: v1
kind: Service
metadata:
  name: nps-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: nps-service, riskshield.io/domain: customer }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50184, targetPort: 50184 }]
  selector: { app.kubernetes.io/name: nps-service }
