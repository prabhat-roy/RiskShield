apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: customer-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: customer
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: customer-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: customer-service, riskshield.io/domain: customer }
    spec:
      serviceAccountName: customer-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: customer-service
          image: harbor.riskshield.internal/riskshield/customer-service:latest
          ports: [{ containerPort: 50180, name: http }]
          envFrom: [{ configMapRef: { name: customer-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
