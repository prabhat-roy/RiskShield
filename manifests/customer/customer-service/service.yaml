apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: customer-service, riskshield.io/domain: customer }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: customer-service }
