apiVersion: apps/v1
kind: Deployment
metadata:
  name: self-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: self-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: customer
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: self-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: self-service, riskshield.io/domain: customer }
    spec:
      serviceAccountName: self-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: self-service
          image: harbor.riskshield.internal/riskshield/self-service:latest
          ports: [{ containerPort: 50182, name: http }]
          envFrom: [{ configMapRef: { name: self-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50182 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50182 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
