apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: document-service, riskshield.io/domain: customer }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50183, targetPort: 50183 }]
  selector: { app.kubernetes.io/name: document-service }
