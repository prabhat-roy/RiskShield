apiVersion: apps/v1
kind: Deployment
metadata:
  name: loss-analytics
  namespace: riskshield
  labels:
    app.kubernetes.io/name: loss-analytics
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: loss-analytics } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: loss-analytics, riskshield.io/domain: analytics }
    spec:
      serviceAccountName: loss-analytics
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: loss-analytics
          image: harbor.riskshield.internal/riskshield/loss-analytics:latest
          ports: [{ containerPort: 50300, name: http }]
          envFrom: [{ configMapRef: { name: loss-analytics } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50300 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50300 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
