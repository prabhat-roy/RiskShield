apiVersion: v1
kind: Service
metadata:
  name: loss-analytics
  namespace: riskshield
  labels: { app.kubernetes.io/name: loss-analytics, riskshield.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50300, targetPort: 50300 }]
  selector: { app.kubernetes.io/name: loss-analytics }
