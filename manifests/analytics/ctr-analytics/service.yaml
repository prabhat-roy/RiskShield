apiVersion: v1
kind: Service
metadata:
  name: ctr-analytics
  namespace: riskshield
  labels: { app.kubernetes.io/name: ctr-analytics, riskshield.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50303, targetPort: 50303 }]
  selector: { app.kubernetes.io/name: ctr-analytics }
