apiVersion: v1
kind: Service
metadata:
  name: cohort-analytics
  namespace: riskshield
  labels: { app.kubernetes.io/name: cohort-analytics, riskshield.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50302, targetPort: 50302 }]
  selector: { app.kubernetes.io/name: cohort-analytics }
