apiVersion: apps/v1
kind: Deployment
metadata:
  name: profitability-analytics
  namespace: riskshield
  labels:
    app.kubernetes.io/name: profitability-analytics
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: profitability-analytics } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: profitability-analytics, riskshield.io/domain: analytics }
    spec:
      serviceAccountName: profitability-analytics
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: profitability-analytics
          image: harbor.riskshield.internal/riskshield/profitability-analytics:latest
          ports: [{ containerPort: 50301, name: http }]
          envFrom: [{ configMapRef: { name: profitability-analytics } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50301 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50301 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
