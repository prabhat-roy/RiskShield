apiVersion: v1
kind: Service
metadata:
  name: profitability-analytics
  namespace: riskshield
  labels: { app.kubernetes.io/name: profitability-analytics, riskshield.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50301, targetPort: 50301 }]
  selector: { app.kubernetes.io/name: profitability-analytics }
