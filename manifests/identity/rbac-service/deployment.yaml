apiVersion: apps/v1
kind: Deployment
metadata:
  name: rbac-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: rbac-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: rbac-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: rbac-service, riskshield.io/domain: identity }
    spec:
      serviceAccountName: rbac-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: rbac-service
          image: harbor.riskshield.internal/riskshield/rbac-service:latest
          ports: [{ containerPort: 50024, name: http }]
          envFrom: [{ configMapRef: { name: rbac-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50024 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50024 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
