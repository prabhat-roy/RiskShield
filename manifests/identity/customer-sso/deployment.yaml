apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-sso
  namespace: riskshield
  labels:
    app.kubernetes.io/name: customer-sso
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: customer-sso } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: customer-sso, riskshield.io/domain: identity }
    spec:
      serviceAccountName: customer-sso
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: customer-sso
          image: harbor.riskshield.internal/riskshield/customer-sso:latest
          ports: [{ containerPort: 50021, name: http }]
          envFrom: [{ configMapRef: { name: customer-sso } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50021 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50021 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
