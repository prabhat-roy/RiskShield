apiVersion: v1
kind: Service
metadata:
  name: customer-sso
  namespace: riskshield
  labels: { app.kubernetes.io/name: customer-sso, riskshield.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50021, targetPort: 50021 }]
  selector: { app.kubernetes.io/name: customer-sso }
