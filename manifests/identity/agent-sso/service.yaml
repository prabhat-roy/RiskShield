apiVersion: v1
kind: Service
metadata:
  name: agent-sso
  namespace: riskshield
  labels: { app.kubernetes.io/name: agent-sso, riskshield.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50022, targetPort: 50022 }]
  selector: { app.kubernetes.io/name: agent-sso }
