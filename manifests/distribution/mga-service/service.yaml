apiVersion: v1
kind: Service
metadata:
  name: mga-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: mga-service, riskshield.io/domain: distribution }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50163, targetPort: 50163 }]
  selector: { app.kubernetes.io/name: mga-service }
