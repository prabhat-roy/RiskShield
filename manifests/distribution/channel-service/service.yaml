apiVersion: v1
kind: Service
metadata:
  name: channel-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: channel-service, riskshield.io/domain: distribution }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50164, targetPort: 50164 }]
  selector: { app.kubernetes.io/name: channel-service }
