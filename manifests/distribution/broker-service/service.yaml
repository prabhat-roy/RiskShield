apiVersion: v1
kind: Service
metadata:
  name: broker-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: broker-service, riskshield.io/domain: distribution }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50161, targetPort: 50161 }]
  selector: { app.kubernetes.io/name: broker-service }
