apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: agent-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: distribution
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: agent-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: agent-service, riskshield.io/domain: distribution }
    spec:
      serviceAccountName: agent-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: agent-service
          image: harbor.riskshield.internal/riskshield/agent-service:latest
          ports: [{ containerPort: 50160, name: http }]
          envFrom: [{ configMapRef: { name: agent-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
