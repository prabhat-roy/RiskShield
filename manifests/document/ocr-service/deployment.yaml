apiVersion: apps/v1
kind: Deployment
metadata:
  name: ocr-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: ocr-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: document
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: ocr-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: ocr-service, riskshield.io/domain: document }
    spec:
      serviceAccountName: ocr-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: ocr-service
          image: harbor.riskshield.internal/riskshield/ocr-service:latest
          ports: [{ containerPort: 50321, name: http }]
          envFrom: [{ configMapRef: { name: ocr-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50321 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50321 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
