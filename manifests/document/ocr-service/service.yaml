apiVersion: v1
kind: Service
metadata:
  name: ocr-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: ocr-service, riskshield.io/domain: document }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50321, targetPort: 50321 }]
  selector: { app.kubernetes.io/name: ocr-service }
