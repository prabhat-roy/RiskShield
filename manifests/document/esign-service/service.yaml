apiVersion: v1
kind: Service
metadata:
  name: esign-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: esign-service, riskshield.io/domain: document }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50323, targetPort: 50323 }]
  selector: { app.kubernetes.io/name: esign-service }
