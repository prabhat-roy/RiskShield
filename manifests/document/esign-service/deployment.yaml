apiVersion: apps/v1
kind: Deployment
metadata:
  name: esign-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: esign-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: document
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: esign-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: esign-service, riskshield.io/domain: document }
    spec:
      serviceAccountName: esign-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: esign-service
          image: harbor.riskshield.internal/riskshield/esign-service:latest
          ports: [{ containerPort: 50323, name: http }]
          envFrom: [{ configMapRef: { name: esign-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50323 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50323 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
