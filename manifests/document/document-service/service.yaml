apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: document-service, riskshield.io/domain: document }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50320, targetPort: 50320 }]
  selector: { app.kubernetes.io/name: document-service }
