apiVersion: apps/v1
kind: Deployment
metadata:
  name: archive-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: archive-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: document
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: archive-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: archive-service, riskshield.io/domain: document }
    spec:
      serviceAccountName: archive-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: archive-service
          image: harbor.riskshield.internal/riskshield/archive-service:latest
          ports: [{ containerPort: 50324, name: http }]
          envFrom: [{ configMapRef: { name: archive-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50324 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50324 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
