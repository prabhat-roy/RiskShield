apiVersion: v1
kind: Service
metadata:
  name: form-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: form-service, riskshield.io/domain: product }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50204, targetPort: 50204 }]
  selector: { app.kubernetes.io/name: form-service }
