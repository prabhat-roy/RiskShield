apiVersion: v1
kind: Service
metadata:
  name: exclusion-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: exclusion-service, riskshield.io/domain: product }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50202, targetPort: 50202 }]
  selector: { app.kubernetes.io/name: exclusion-service }
