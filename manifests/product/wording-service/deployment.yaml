apiVersion: apps/v1
kind: Deployment
metadata:
  name: wording-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: wording-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: product
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: wording-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: wording-service, riskshield.io/domain: product }
    spec:
      serviceAccountName: wording-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: wording-service
          image: harbor.riskshield.internal/riskshield/wording-service:latest
          ports: [{ containerPort: 50203, name: http }]
          envFrom: [{ configMapRef: { name: wording-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50203 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50203 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
