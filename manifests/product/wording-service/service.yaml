apiVersion: v1
kind: Service
metadata:
  name: wording-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: wording-service, riskshield.io/domain: product }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50203, targetPort: 50203 }]
  selector: { app.kubernetes.io/name: wording-service }
