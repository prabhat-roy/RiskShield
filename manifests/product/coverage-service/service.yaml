apiVersion: v1
kind: Service
metadata:
  name: coverage-service
  namespace: riskshield
  labels: { app.kubernetes.io/name: coverage-service, riskshield.io/domain: product }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: coverage-service }
