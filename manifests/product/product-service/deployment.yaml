apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: riskshield
  labels:
    app.kubernetes.io/name: product-service
    app.kubernetes.io/part-of: riskshield
    riskshield.io/domain: product
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: product-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: product-service, riskshield.io/domain: product }
    spec:
      serviceAccountName: product-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: product-service
          image: harbor.riskshield.internal/riskshield/product-service:latest
          ports: [{ containerPort: 50200, name: http }]
          envFrom: [{ configMapRef: { name: product-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
