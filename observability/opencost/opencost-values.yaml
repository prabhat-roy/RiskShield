opencost:
  prometheus: { internal: { enabled: false }, external: { url: http://prometheus.observability:9090 } }
  ui: { enabled: true }
  exporter:
    cloudProviderApiKey: ""
    defaultClusterId: riskshield-prod
ingress:
  enabled: true
  hosts: [{ host: opencost.riskshield.internal, paths: [{ path: /, pathType: Prefix }] }]
