# OpenCost — per-namespace / per-team / per-service cloud cost attribution
image: { repository: quay.io/kubecost1/kubecost-cost-model, tag: latest }
opencost:
  prometheus: { external: { enabled: true, url: http://mimir-querier.riskshield.svc:8080/prometheus } }
  exporter: { defaultClusterId: riskshield-prod }
